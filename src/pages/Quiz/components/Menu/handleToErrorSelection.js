const handleToErrorSelection = () => {
    const messageErreur = document.querySelector('.error-message-selection');
    messageErreur.classList.remove('hidden-text-selection');
};

export default handleToErrorSelection;
